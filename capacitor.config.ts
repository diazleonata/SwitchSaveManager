import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.switchsavemanager',
  appName: 'SwitchSaveManager',
  webDir: 'dist'
};

export default config;
